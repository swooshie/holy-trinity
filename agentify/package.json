{
  "name": "agentify-valiron",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "check": "npm --prefix backend run check && npm --prefix backend test && npm --prefix frontend run build",
    "smoke": "npm --prefix backend run smoke",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  }
}
